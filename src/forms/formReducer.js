// Reducer for the 'form' part of the Redux state, and
// selector functions to get data from it

// The selector functions assume that this part of the data is under
// the key 'form' in the main state.


export const formReducer = function(state={}, action) {
  return state;
}
